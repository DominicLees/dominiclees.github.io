head
    title Dom's Blog
    link(rel="stylesheet", href="styles/master.css")
body
    h1 Dom's Blog
    for article in articles
        div
            a(href=`./post/${article.filename}`) 
                h2= article.title
                p= article.subtitle